name: Second Workflow

on: push

jobs:
  second-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "This is the second workflow"

      - name: Show system info
        run: uname -a
